apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: three-tier
  labels:
    service: web
spec:
  selector:
    service: web
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP