apiVersion: v1
kind: Service
metadata:
  name: ratings
  namespace: three-tier
  labels:
    service: ratings
spec:
  selector:
    service: ratings
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP
